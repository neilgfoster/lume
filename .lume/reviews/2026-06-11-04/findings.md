# Review findings - 2026-06-11-04

Provenance: lume review | 2026-06-11 | automated self-review (deep adversarial swarm, 8 lenses + per-finding skeptic verification), not external validation

## Direction decisions

### README.md line 39 still states '- **It is v0.1.0.** Expect rough edges and breaking changes.' while plugin/.claude-plugin/plugin.json and pyproject declare version 0.2.0; workstream 0022 (release-0-2-0) is closed and v0.2.0 was tagged and merged to main (commit 1346d78). The version-consistency test (tests/test_version_consistency.py) only checks plugin.json and pyproject.toml, never README prose, so the stale string was never caught. Verdict skeptic lowered severity to medium: line 39 sits in the 'Why you should NOT use lume' maturity-warning section, so it reads as conservative caution rather than a misleading capability claim; it is documentation debt, not a functional defect. Not flagged in any prior gap/decision/review.

**Decision:** Treat the stale 'v0.1.0' README string as documentation debt to be corrected when README counts/version are refreshed (see proposed doc-freshness workstream), not as a separate roadmap item. Do not over-read the secondary complaint that the README feature list omits hierarchical workstreams / machine-verifiable DoD / cross-repo gaps: the README is deliberately evidence-conservative and v0.2.0's actual scope is the review-verb era and gap lifecycle.

**Rationale:** A released version undocumented in the README is real inconsistency, but it lives in a conservative maturity warning and carries no functional or capability-claim risk. Folding it into the doc-freshness fix avoids ceremony while keeping the correction honest.

### README.md lines 28-30, 53, 157 and docs/vision.md line 57 carry a [U] (unevidenced) tag claiming 'The install has never run in a live Claude Code session... verified only by an automated smoke test'. Workstream 0011's retro (P5.4, retro.json line 47) records an internal, verifiable live install: a human ran /plugin marketplace add + /plugin install then lume seed/status/check in the tredl adopter repo on 2026-06-11. Per the README's own [E]/[U] convention this should be [E]. The 0011 retro even asserts 'the prior [U] caveat is now flipped to verified in the README' — but that edit never landed, so the project state is internally self-contradicting. README was last edited inside workstream 0011 (commit 23dbeca), the same workstream that produced the retro.

**Decision:** Flip the live-install caveat from [U] to [E] in README.md (lines 28-30, 53, 157) and docs/vision.md line 57, citing the 0011 retro P5.4 evidence, as part of the doc-freshness workstream. Do not invent an external-validation claim — the evidence is internal (operator-run in the tredl repo), so keep the [E] citation pointed at the 0011 retro.

**Rationale:** The README's entire credibility pitch rests on its [E]/[U] tagging being honest. A [U] that the project's own accepted state has already flipped to [E] — and a retro that wrongly claims the edit landed — is a real, internally self-contradicting honesty gap. The fix is straightforward because the evidence already lives in lume's own state.

### ECOSYSTEM-FIT OVERLAP AUDIT (live lookup, June 2026). lume's shipped surface was enumerated: plugin/.claude-plugin/plugin.json, .claude-plugin/marketplace.json, plugin/skills/lume/SKILL.md, plugin/bin/lume (python src-layout entry), 25 python modules, 11 JSON schemas. Confirmed ABSENT from the shipped plugin: plugin/hooks, plugin/commands, plugin/agents, .mcp.json (the repo's require-workstream-commit.py hook lives in .claude/, not plugin/, and is repo discipline, not a lume feature). PER-NATIVE-FEATURE GRADE: (a) Skills — lume ships ONE skill wrapping a deterministic CLI; NO OVERLAP. (b) Subagents — separate-context workers; lume is orthogonal state; NO OVERLAP. (c) Plan mode — ephemeral read-only session; lume's discovery/planning iterations are persisted JSON; PARTIAL/conceptual only. (d) Tasks/Todo (native v2.1+, persisted to ~/.claude/tasks/, cross-session, dependency graph, blocked/pending/in_progress/completed, broadcasts across sessions) — STRONGEST overlap on persisted cross-session tracking with dependencies, BUT Tasks have NO human accept/reject gate and no deterministic non-LLM transition control; PARTIAL OVERLAP, gate is the differentiator. (e) Hooks — lume the engine ships none; NO OVERLAP. (f) Memory — orthogonal; NO OVERLAP. (g) Dynamic Workflows (v2.1.154+, JS orchestrating subagents, resumable in-session) — the doc's own limits table states 'No mid-run user input — for sign-off between stages, run each stage as its own workflow'; autonomous orchestration with one launch-time approval, NOT per-stage operator gates; PARTIAL OVERLAP on codified repeatable orchestration, differs sharply on operator-gated-per-stage and deterministic-state-not-LLM-script. PER-OFFICIAL-PLUGIN GRADE (anthropics/claude-plugins-official, ~35 listed, 12 internal per DeepWiki): all language-LSP plugins, code-review/pr-review-toolkit, plugin-dev/skill-creator, feature-dev, session-report, mcp-* — NONE track gated workstreams or persist operator-gated iteration state; NO OVERLAP. ralph-loop (the only loop/workflow plugin) — an AUTONOMOUS self-driving loop (Stop hook re-feeds the same prompt until a completion string); no operator approval gates, no accept/reject, no persisted inspectable JSON state, no workstreams; lume's polar opposite; NO OVERLAP (confirms the wedge). PLUGIN-STRUCTURE CHECK (plugins-reference, full 76KB): plugin.json/marketplace.json valid; SKILL.md sets name per guidance; single-skill skills/lume/ layout valid; reference documents NO 'bin/ auto-added to PATH' mechanism (bundled executables are referenced via ${CLAUDE_PLUGIN_ROOT}); grep confirmed only ${CLAUDE_PLUGIN_ROOT} patterns and the LSP install-separately note.

**Decision:** RE-AFFIRM (STANDING): the irreducible wedge survives unchanged — lume is the only operator-gated, deterministic (non-LLM), persisted-inspectable-JSON workstream/iteration engine. Native Tasks and Dynamic Workflows are the closest neighbours but both lack the per-stage human accept/reject gate and deterministic non-LLM state transitions; ralph-loop is the explicit opposite. No adoption or deprecation action follows.

**Rationale:** The deprecation rule was applied per-item and honestly: the only PARTIAL overlaps (Tasks, Plan mode, Dynamic Workflows) all fail on lume's defining differentiator (operator-gated, deterministic, inspectable state), and every official plugin scored NO OVERLAP. The wedge is not duplicated by any current native feature or first-party plugin. This preserves the itemised audit trail that the prior review failed to record (gap G13).

### STANDING (re-confirmed, gap G10 follow-up). The plugins-reference confirms a Claude Code plugin MAY bundle agents/, hooks/, .mcp.json, and monitors/. lume ships none of these (verified: no plugin/agents, plugin/hooks, plugin/commands, or .mcp.json anywhere). No gap record in .lume/gaps/ (G1-G14) demonstrates demand for a review-agent or MCP server; grep for agent/mcp/subagent returns nothing. The charter (protocol ~line 1060) explicitly biases simple-and-usable-now and names speculative surface as the trap to avoid (P6 'DO NOT SCHEDULE'). This re-affirms the second UNADOPTED ecosystem decision from review 2026-06-11-01, also re-affirmed in 2026-06-11-03.

**Decision:** DO NOT build the agents/ or MCP-server surface speculatively. Re-affirm as STANDING; do not schedule. Revisit only if a gap record demonstrates concrete demand.

**Rationale:** Shipping an agents/ dir or MCP server now would contradict lume's own anti-scope-creep discipline and the very wedge above (deterministic, non-LLM control). The restraint remains correct: there is no evidenced demand, and the structural opportunity stays open at zero cost.

## Proposed workstreams

### value-signal: Capture the value signal: reject-rate and gate-friction instrumentation from lume's own state [critical path]

Serves goal: Validate (or falsify) lume's single biggest unproven claim — that the ceremony pays for itself — with data drawn from lume's own deterministic state rather than anecdote.

lume's headline value proposition ('ceremony pays for itself') currently rests on qualitative retro notes, not measured reject-rate or gate-friction data. The protocol itself (lines 922-930) marks reject-rate and ceremony-cost as UNMEASURED and names this 'lume's single biggest unproven claim / the headline open question'. There are no counters, timers, or metrics anywhere in cli/handlers or models. This workstream adds lightweight, deterministic measurement derived from already-persisted iteration/transition state (no LLM, no network, no new ceremony in the loop) so the value claim becomes auditable over time. This is the STANDING value-signal proposal from reviews 2026-06-11-01 and -03, re-confirmed UNADOPTED.

- (spike, committed) Spike: define a deterministic value-signal derivable purely from existing state (e.g. reject count / accept count per workstream, count of accept->redo cycles, iterations-per-workstream) read from persisted iteration transitions — no new counters in the hot path.
  - evidence: Protocol lines 922-930 mark reject-rate and ceremony-cost UNMEASURED; line 928 'lume's single biggest unproven claim ... the headline open question'. grep across plugin/ for reject_rate/ceremony/metric/counter/timer found only prose prompt strings and store.py's _counter (an ID-sequence generator), confirming no value metric exists.
- (slice, committed) Slice: a read-only `lume value` (or extend `lume status`) report that computes reject-rate and gate-friction from persisted transition history, all dates via the existing clock seam, deterministic and offline.
  - evidence: Workstream retros (protocol lines 31, 73, 140) report value anecdotally ('net positive', 'clearest payoff') with no numbers; the value-signal proposal (protocol lines 1209, 1213) is UNADOPTED across two prior reviews.
- (chore, optional) Chore: document the measured baseline in README/docs to replace the anecdotal 'net positive' phrasing with an evidenced [E] figure, and wire it into the doc-freshness guard so it cannot silently drift.
  - evidence: README/docs currently assert value qualitatively; the same drift mechanism that staled the test/workstream counts (see doc-freshness workstream) would stale any hand-written value figure.

### doc-freshness-guard: Re-anchor drifted README/docs claims and wire a CI freshness guard [critical path]

Serves goal: Protect lume's flagship discipline — leading with disqualifiers and evidencing every claim — by making its own [E]-tagged self-evidence figures true and keeping them true automatically.

Multiple [E]-tagged claims on lume's primary credibility surface have drifted from reality, twice across two review cycles, because no prose-count CI guard exists (the version-consistency test only checks plugin.json/pyproject, not README prose). Concretely: test count claims '313' (README:47,156) vs actual 427; workstream count claims 'eight closed ... a ninth in progress' (README:54-55,158; docs/vision.md:53-54; docs/constraints.md:4-5; docs/questions.md:41) vs actual 22 closed + 1 active; adopter wording is internally inconsistent (README:18 'zero external adopters' and :120 'currently: just lume' vs ADOPTERS.json's 2 rows and README:70's 'the tredl adopter repo'). Also fold in the stale 'v0.1.0' string (README:39) and the live-install [U]->[E] flip (README:28-30,53,157; docs/vision.md:57). This workstream corrects every drifted claim and wires a deterministic CI guard so the figures cannot silently drift again.

- (slice, committed) Slice: correct the drifted figures in README and docs — test count 313->427, workstream count 8+1->22 closed+1 active (README:54-55,158; docs/vision.md, docs/constraints.md, docs/questions.md), reconcile the adopter wording against ADOPTERS.json, flip the live-install caveat [U]->[E] citing 0011 retro P5.4, and update the stale 'v0.1.0' string to 0.2.0.
  - evidence: pytest --collect-only reports 427; README.md:47,156 say '313'. .lume/workstreams/ holds 22 closed + 1 active; README.md:54-55,158 and docs/{vision,constraints,questions}.md say '8 closed + 1 in progress'. ADOPTERS.json lists lume+tredl while README:18/:120 say zero/just-lume and README:70 references the tredl adopter repo. retro.json line 47 (0011 P5.4) records a human-run live install on 2026-06-11. plugin.json/pyproject=0.2.0 while README:39 says v0.1.0.
- (slice, committed) Slice: wire a deterministic CI freshness guard (extend the version-consistency test pattern) that re-derives test count, closed/active workstream count, and adopter count from source and fails CI when README/docs prose disagrees.
  - evidence: tests/test_version_consistency.py checks only plugin.json and pyproject.toml, never README prose, which is why v0.1.0 and the counts drifted uncaught; the same drift recurred across reviews 2026-06-11-01/-03, evidencing a systemic gap not a one-off.
- (chore, optional) Chore: add a short docs note that all [E]-tagged count/version claims are CI-enforced, so future doc edits know the figures are machine-checked.
  - evidence: README:14-15,163 stake the project's honesty pitch on [E]='evidenced with a citation'; making the enforcement visible closes the loop between the policy and its mechanism.

### review-protocol-auditability-and-scale: Make the review protocol auditable and scale-resilient (meta-gaps M2/M3/M4 + lens additions)

Serves goal: Strengthen lume's own review machinery so its conclusions are reproducible, scale gracefully as the corpus grows, and preserve the evidence trails whose absence caused prior review failures.

The deep-adversarial meta lens surfaced several design gaps in the review protocol/result contract itself. The two highest-value, recurrent ones: (M3/G13) the ecosystem-fit lens requires an itemised native-feature/official-plugin overlap audit but the result contract has no field to record the matrix, so prior reviews recorded only a bare conclusion and the audit was unreproducible — this recurred across two reviews; and (M4/G11) the protocol embeds every workstream's full objective/decisions/plan/retro with NO cap (gather_charter in plugin/src/lume/review.py iterates all workstreams unbounded; ~1294 lines this run, 22 workstreams) while charter docs are capped (MAX_DOC_FILES=12, MAX_DOC_CHARS=8192), so reviewer cognitive load degrades monotonically as the project matures. Lower-value-but-real residue (M2 null-check trail, M5 steelman audit, M6 a dedicated structural-simplification/refactor lens distinct from value/viability, M8 capturing steelmanned-away near-findings) is bundled as optional follow-on.

- (slice, committed) Slice: add a structured field to the review_result contract (and schema) for the ecosystem-fit overlap audit — a per-item matrix of {native feature | official plugin} x {overlap grade | rationale} — so the surviving-wedge conclusion always ships with its supporting audit. Closes G13.
  - evidence: plugin/src/lume/review.py lines 61-74 instruct the reviewer to itemise native features and first-party plugins and conclude with the surviving wedge, but result_contract_skeleton() (lines ~220-238) and plugin/src/lume/schemas/review_result.json have no matrix/overlap/grade field (grep returns nothing). Gap lume-0013 is open, resolution null; .lume/reviews/2026-06-11-03 re-affirmed the conclusion in prose with no per-item enumeration.
- (slice, committed) Slice: bound the embedded charter — cap or summarise older workstreams (e.g. title + retro verdict only for older ones, full text for recent N) so review context stops growing monotonically. Closes/mitigates G11.
  - evidence: plugin/src/lume/review.py gather_charter (lines ~148-163) appends objective/decisions/plan/retro for EVERY workstream with no cap, while MAX_DOC_FILES=12 / MAX_DOC_CHARS=8192 (lines ~27-28) bound only charter docs; gap lume-0011 (open, resolution null) states review cost 'degrades monotonically as the project matures'.
- (spike, optional) Spike: evaluate adding a structured verification/evidence-trail field (M2) recording which checkable claims were re-derived, when, and the outcome — weighing it against the unbounded-context cost it would add (null checks).
  - evidence: review.py:57-60 instructs 'mechanically re-derive every checkable one BEFORE grading' but the result contract has no field for null-check outcomes; findings-producing checks ARE baseline-compared via the prior-review follow-up (e.g. the live 427-vs-401 test-count comparison), so only null checks leave no trace.
- (spike, optional) Spike: evaluate a dedicated structural-simplification/refactor lens (M6) distinct from value/viability, and a steelman/near-finding audit trail (M5/M8), against the protocol's deliberate single-pass minimalism.
  - evidence: value/viability lens (review.py:75-78) frames keep/delete worth, not consolidate-without-behaviour-change; ecosystem-fit redundancy is external-only; the result schema has no rejected_findings/steelman_reasoning field (review.py:320-321 discards steelmanned-away findings with no capture). The repo's /simplify and /code-review skills are diff-scoped, not whole-repo.

## Review gaps (META lens - gaps in this review itself)

### docs/scope.md line 64 (the 'Reconciliation: plan vs what shipped' section, dated 2026-06-10) asserts 'Decomposition / nesting — not built (still the first thing cut)', but the engine SHIPS decomposition: plugin/src/lume/cli/handlers/lifecycle.py has handle_spawn, the verb is wired in handlers/__init__.py and catalog.py, workstream.py exposes a parent property, the workstream schema has a parent field, and workstream 0011 iterations P19-P22 (built parent-ref+spawn, rollup, close/reopen guards) were accepted with the suite green. This is a live, misleading inaccuracy in a core scope doc, not a clean logged decision change — the doc denies the build rather than acknowledging the revision.

Why missed: Goal-fidelity originally framed this as an honest logged decision change (low). Adversarial verification found the opposite: scope.md's own reconciliation still claims the feature was NOT built, contradicting the shipped engine. The original finding's evidence was also largely wrong (claimed .lume/workstreams/0016/state.json has parent:0015 — false; no workstream has any parent set; and cited an unverifiable 'protocol line 470+').

Proposed change: Correct docs/scope.md line 64 to acknowledge decomposition (parent field + spawn verb + rollup + close-guards) shipped in workstream 0011 (P19-P22, accepted), re-anchoring the evidence to docs/scope.md:64 and the 0011 retro. Fold this correction into the doc-freshness workstream's doc-currency pass.

### The review/gap machinery (workstreams 0015-0021: review verb, gaps relocation, review landing, gap linking, gap naming, review-output-lifecycle, review-protocol) consumed ~14 iterations (about 11.5% of the project's ~122 iterations) building deterministic, load-bearing review-and-gap systems, yet every one of the 14 auto-captured gaps (G1-G14) and all three review runs are internal to lume's own development. The two adopters (ADOPTERS.json: lume, tredl) are both the operator's own projects; tredl has filed zero gaps and no external adopter has used the review verb. The investment is defensible but unvalidated on a real external audience.

Why missed: Not a code defect, so the code-focused lenses would not flag it; it is a value-allocation observation that only surfaces when iteration spend is tallied against the (zero external) usage base. The original finding undercounted (said 12 gaps / 10 iters / 8.2%); verification corrected it to 14 gaps (G1-G14) and ~14 iterations (~11.5%), all internal.

Proposed change: Record this as a standing value-viability watch item (not a defect): the review/gap machinery is sunk, real, and lightweight, but its worth is unproven until an external adopter (or tredl) actually files a gap or runs a review. Tie its validation to the value-signal workstream and to the first external/tredl-originated gap; revisit the allocation if none materialises.

### STANDING (re-confirmed): lume's headline 'ceremony pays for itself' claim has no instrumentation — no reject-rate, gate-friction, or ceremony-cost measurement exists anywhere in the engine. The protocol itself (lines 922-930, line 928) names this 'lume's single biggest unproven claim / the headline open question'. This is the UNADOPTED value-signal proposal from reviews 2026-06-11-01 and -03, surfaced per protocol line 1217 which prescribes reporting each UNADOPTED item as a standing finding.

Why missed: Not missed — deliberately deferred. It is re-surfaced here (rather than silently dropped or re-proposed as new) because the prior-review follow-up loop and protocol line 1217 require standing UNADOPTED items to be re-affirmed with fresh evidence each cycle. Fresh evidence: grep across plugin/ confirms still no counters/timers/metrics (only prose prompt strings and store.py's ID-sequence _counter).

Proposed change: Re-affirm STANDING and convert to action this cycle via the proposed value-signal workstream (above), which derives the signal deterministically from already-persisted state. Until then, keep README/docs value claims qualitative and unembellished rather than asserting an unmeasured payoff.

### STANDING (re-confirmed): the structural opportunity to ship an agents/ directory or an MCP server remains open and unjustified. The plugins-reference confirms plugins MAY bundle agents/, hooks/, .mcp.json, and monitors/; lume ships none (verified absent: plugin/agents, plugin/hooks, plugin/commands, .mcp.json). No gap record (G1-G14) demonstrates demand. This re-affirms the second UNADOPTED ecosystem decision from review 2026-06-11-01 (also re-affirmed -03), surfaced via the prior-review follow-up loop (gap G10).

Why missed: Not missed — a deliberate restraint re-confirmed each cycle per the standing-findings instruction. Fresh evidence: find on plugin/ confirms the four surfaces are still absent; grep for agent/mcp/subagent in .lume/gaps/ returns nothing, so no demand has materialised.

Proposed change: Re-affirm STANDING; do not schedule. Building speculatively would violate lume's anti-scope-creep discipline (protocol ~line 1060, P6 'DO NOT SCHEDULE') and the deterministic-non-LLM wedge. Revisit only if a gap record demonstrates concrete demand.

### M2: the review result contract has no field to record an evidence/verification trail for null checks — checks the reviewer ran that found nothing. A future reviewer re-running the same check cannot tell whether they are confirming a prior verification or duplicating work.

Why missed: The result contract was designed to record findings (the assumption being that findings ARE the evidence). Adversarial verification narrowed the scope: findings-producing checks ARE re-derived and baseline-compared via the prior-review follow-up mechanism (demonstrated by the live 427-vs-401 test-count comparison), so 'not auditable' was overstated; only null checks leave no trace, which is minor.

Proposed change: Tracked as an optional spike in the review-protocol-auditability-and-scale workstream. Weigh capturing null-check outcomes against the unbounded-context cost they would add (logging every null verification would worsen the M4/G11 context-bloat gap); it may be correct to leave null checks out by design.

### M5/M8: the steelman filter is a critical, adversarial-review gate ('only findings that survive the steelman go in the result', review.py:320-321), but the result contract has no field to record steelmanned-away or near-proposed findings or the reasoning for rejection. A future reviewer cannot tell whether a non-finding was properly steelmanned or simply overlooked, and there is no re-surfacing mechanism for discarded candidates (unlike UNADOPTED items, which the follow-up loop re-surfaces).

Why missed: The protocol emphasises steelmanning in instructions but designed the result contract to capture only what survives, treating the steelman as a thought process rather than an artifact. Verification confirmed the asymmetry: review_gaps carry a why_missed trail for surviving findings, but rejected candidates leave none; distinct from the adjacent gap lume-0014 (multi-viewpoint swarm) which proposes a structurally different remedy.

Proposed change: Tracked as an optional spike in the review-protocol-auditability-and-scale workstream: evaluate a lightweight 'steelmanned-away / near-finding' note (capture the rejection rationale so future reviews can re-check whether it still holds) against the protocol's deliberate single-pass minimalism. Low priority — a discarded finding can be independently re-found by a future thorough review.

