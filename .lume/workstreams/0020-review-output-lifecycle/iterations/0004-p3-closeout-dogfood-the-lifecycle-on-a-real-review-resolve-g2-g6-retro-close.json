{
  "dod": {
    "items": [
      {
        "checked": false,
        "text": "Dogfood: a real lume review run end to end through the new lifecycle - emit, an honest delta review against the morning's review-2026-06-11-01 (deduped against current plans/decisions/open gaps; thin findings are acceptable if true), then ingest with an owning workstream; the new folder lands under .lume/reviews/ with the owner recorded in its result.json",
        "check": {
          "kind": "command",
          "cmd": "python3 -c \"import json,glob; fs=sorted(glob.glob('.lume/reviews/*/result.json')); assert len(fs)>=2, fs; d=json.load(open(fs[-1])); assert d['sections'][0]['heading']=='workstream', d['sections'][0]; print('ok')\""
        }
      },
      {
        "checked": false,
        "text": "G2 and G6 resolved via the structured resolve, linked to this workstream (0020)",
        "check": {
          "kind": "command",
          "cmd": "python3 -c \"import json,glob; recs=[json.load(open(f)) for f in glob.glob('.lume/gaps/lume-000[26]-*.json')]; assert len(recs)==2 and all(r['status']=='resolved' and '0020' in r['workstreams'] for r in recs); print('ok')\""
        }
      },
      {
        "checked": false,
        "text": "Retro written (lume retro): did the lifecycle change buy its cost? Honest verdict including what the dogfood did NOT exercise; carry-forwards recorded",
        "check": {
          "kind": "command",
          "cmd": "python3 -c \"import json; d=json.load(open('.lume/workstreams/0020-review-output-lifecycle/retro.json')); assert d['overall_verdict'] and 'draft' not in d['overall_verdict']; print('ok')\""
        }
      },
      {
        "checked": false,
        "text": "Objective done-when assessed clause by clause (decisions logged; G2+G6 designed, built, tested, documented, dogfooded) and recorded via lume decide; all review/gap artifacts committed",
        "check": {
          "kind": "command",
          "cmd": "test -z \"$(git status --porcelain .lume/gaps .lume/reviews)\""
        }
      },
      {
        "checked": false,
        "text": "Workstream handed off for operator accept; lume close runs only on the operator's accept of this iteration (close is the operator's call)"
      }
    ],
    "preamble": "P3 closeout: dogfood the new review lifecycle for real, resolve the answered gaps, retro honestly, and hand the workstream off for close."
  },
  "handback": null,
  "id": 4,
  "self_review": null
}
