---
id: 006
type: execution
phase: handback
opened: 2026-06-09
---
# Iteration 006 - P4: rewire lifecycle verbs to state.json + regenerate views

## DoD
Rescoped to P4a after the approved plan split (decisions.md, 006): this slice is
the behaviour-preserving data-first bridge; P4b does the source-of-truth flip.

- [x] A canonical strict verdict parser lives in iteration.py (the stamp regex
      moved here from migrate); migrate re-uses it (no duplicated regex). The
      strict format `YYYY-MM-DD | ACCEPTED|REJECTED [| reason]` is unchanged.
- [x] `Iteration` carries verdicts as data: a `verdicts` field (default None ->
      parse from body via verdict_list(), preserving today's behaviour) and
      `accepted_on()` routes through it rather than ad-hoc body scanning.
- [x] `Iteration.to_entity()` returns the P1 iteration entity dict; `from_entity(d)`
      rebuilds an Iteration such that title + accepted_on work. Round-trip
      to_entity(from_entity(d)) == d is tested.
- [x] `migrate._iteration_entity` reimplemented on top of `to_entity()` (keeping
      only the legacy-type normalisation); the parallel construction + duplicate
      regex are gone.
- [x] Behaviour preserved: NO verb/Workstream/Repository/snapshot change. All
      pre-existing tests pass unchanged; `lume migrate` byte-identical for the two
      CLOSED workstreams (state-as-data legitimately changed - 005 accepted, 006
      added, plan edited).
- [x] New tests (6) cover the verdict parser, verdicts field/accepted_on, and the
      to_entity/from_entity round-trip. Full suite green (109).
- [x] No dead code / unused imports; no duplicated parsing left behind.
- [x] BONUS (caught in verification): renumbered plan P4a/P4b -> P4/P5 because the
      plan parser only matches numeric P<n> and was silently dropping the lettered
      ids. Logged in decisions.md.

## Self-review
Adversarial pass against the (rescoped P4a) DoD:
- Behaviour-preservation is the core claim and it is verified the strict way: all
  103 pre-existing tests pass UNCHANGED (I added 6, didn't edit old ones), and the
  two closed workstreams migrate byte-identically. The refactor is provably inert.
- The verdicts-field design preserves the markdown path (field defaults None ->
  parse body) while enabling the state path (field set -> authoritative). A test
  pins that the field wins over the body when both exist - the behaviour P5 needs.
- DRY achieved without a dangling re-export trap: the one regex lives in
  iteration.py; migrate re-exports parse_verdicts only because a P3 test asserts
  migrate.parse_verdicts - load-bearing, marked noqa, not accidental.
- Caught a real silent bug during verification, not by luck but by checking the
  derived output: "P4a"/"P4b" plan ids were dropped by the P<n>-only parser. Fixed
  by renumbering to P4/P5 and logged. This is exactly the class of failure the
  workstream exists to kill (state that looks fine but is silently malformed) -
  here it bit the plan's own format, and validation/derivation surfaced it.
- Scope honestly held: zero behaviour change to verbs/Workstream/Repository. The
  flip is entirely P5. from_entity is added but not yet consumed by any verb.

## Handback
Rescoped 006 to P4a after the approved split (P4 monolith -> P4 model bridge + P5
flip; logged). Moved the canonical strict verdict parser into iteration.py; added
an Iteration `verdicts` field + `verdict_list()`, routed `accepted_on()` through
it, and added `to_entity()`/`from_entity()` with a tested round-trip. Reimplemented
`migrate._iteration_entity` on top of `to_entity()`, deleting the duplicate regex
and parallel construction. Behaviour preserved: 103 prior tests pass unchanged + 6
new (109 total); closed-workstream migration byte-identical. During verification
caught and fixed the plan parser silently dropping lettered ids (P4a/P4b -> P4/P5).
No verb/Repository/snapshot change. Next: P5 flips Workstream to read/write
state.json and regenerate views, consuming from_entity.

## Verdict
(operator: accept / reject + reasons)
