# lifecycle - decisions

Append-only. Newest at the bottom. Plan changes are logged here with their why
(per decision e).

- 2026-06-09 | (003 planning) (a) Representation = B: per-iteration `type`, not a workstream-level phase machine | Smallest kill of the `type: build` hardcode; no second state machine; the workstream "phase" is derived from the active iteration, not stored (echoes 004 deriving Done/Now). Revisit A only if a named puller needs stored workstream phase the snapshot can't give. (from discovery.md §2)
- 2026-06-09 | (003 planning) (b) The iteration `type` vocabulary is a property of the practice template, not a fixed engine enum | Default (delivery-style) set = {discovery, planning, execution, closeout}; a template may declare its own (e.g. research adds research, dispose). Hardcoding exactly four would leak when the second template arrives (discovery.md §4 leak).
- 2026-06-09 | (003 planning) (c) Stage ordering = convention carried by the consent gate, NOT a separate phase gate | The open/accept consent gate already controls progression ("no crossing a boundary without consent"); a second ordering gate is ceremony the constitution would question. The living plan expresses intended order; consent enforces it; stages may recur (discovery spike mid-execution) without phase thrash.
- 2026-06-09 | (003 planning) (d) Per-type DoD-skeletons = yes, as data, in a dedicated later slice (P4) | Each type pre-loads a default DoD/body skeleton on open - this is what makes typed iterations buy back DoD-drafting time. Independent of un-hardcoding type, so a separate iteration. Committed, not first.
- 2026-06-09 | (003 planning) (e) Plan-tracking = living plan.md; engine derives snapshot Next + "step N of M" from it; each plan item links to its iteration; plan changes = edit plan.md + a logged why here | The Next-analogue of 004's derive-Done/Now. Direct evidence it is needed: hand-authored `## Next` drifted and was re-fixed after every accept this session. Snapshot `## Next` becomes a derived view once P3 ships.
- 2026-06-09 | (003 planning) (f) Multi-workstream = queue + explicit/default target; retire `.lume/current` | In-progress = all `status: active`; commands target a named workstream (proposed `-w <slug>`), defaulting when exactly one is active and erroring (listing actives) when omitted with >1; `lume status` becomes the cross-workstream review queue. Fits the parallel-workstreams + flat-queue vision; removes the global mutable cursor and its cross-session stomp.
- 2026-06-09 | (008 close-out) lifecycle CLOSED - objective met | Typed/phased iterations run (006-008 typed; workstream ran discovery->planning->execution->closeout); tooling tracks active/closed + >1 workstream via the queue + `-w` (the objective's "current" evolved into the queue, decision f); the workstream was run through the lifecycle end to end. See retro.md. Net positive (clearest win: derived Next ended the hand-maintained-Next drift).
- 2026-06-09 | (008 close-out) Carry-forward to a future workstream | P5 contract proof (templates-as-data + two live templates); `lume use` to switch among multiple active workstreams; auto-writeback of plan `iter:` links; queue only lived-tested with one active workstream; multi-day-gap re-orientation still unproven; review contract seam unbuilt; queue surfaces only `handback`, not `accepted`-ready-to-open.
- 2026-06-09 | (after 007) Plan change: P5 (templates + live contract proof) CUT, moved to Deferred | Operator call at the plan's designated optional point. The objective's done-when is met by P1-P4 + P6; P5 is the largest item and the constitution favours the cut; the contract design test already passed on paper (discovery.md §4). Revisit only when a second real practice template is needed. (Logged per decision e: plan changes carry their why.)
- 2026-06-09 | (003 planning) SUPERSEDES 001: the single `.lume/current` pointer is retired by (f) | 001 shipped single-current as a one-workstream-era convenience. It does not fit the original multi-parallel-workstream goal (vision.md). The `status: active|closed` field and `lume new`/`close` from 001 are kept; only the current-cursor selection is replaced (by the queue + `-w` target). Honest reversal, logged.