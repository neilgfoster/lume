---
id: 004
type: execution
phase: accepted
opened: 2026-06-09
---
# Iteration 004 - Queue + multi-workstream rework (plan P1)

## DoD

- [x] 1. `-w <slug>` target selector (alias `--workstream`), parseable anywhere in argv: any verb acting on an existing workstream resolves its target from it. (Evidence: `lume -w lifecycle status` and `lume status -w lifecycle` both resolve lifecycle.)
- [x] 2. Default to the sole active workstream when `-w` is omitted: exactly one `status:active` -> use it; zero active -> clear error (exit 1); two+ active -> error (exit 1) listing the active slugs and instructing `-w`. (Evidence: sole-active resolves with no -w; a 2-active fixture with no -w -> error naming both.)
- [x] 3. Bad targets refused: `-w` naming a closed or nonexistent workstream -> clear named error, exit 1. (Evidence: both cases.)
- [x] 4. `.lume/current` retired (not orphaned): the pointer file is removed; selection neither reads nor writes it; `lume new` creates an active workstream without a cursor; `lume close` flips status only. The pointer code (`current_slug`/`_set_current`/`CURRENT_FILE`) is deleted, not left dead. (Evidence: file absent; grep shows no current-pointer code; new/close behaviour.)
- [x] 5. `lume status` (no `-w`) = cross-workstream review queue: an AWAITING YOU section (active workstreams whose latest iteration is in `handback`, with id), an IN PROGRESS section (other active + latest phase / "no iterations"), and a CLOSED list. (Evidence: a fixture spanning all three -> buckets correct.)
- [x] 6. `lume status -w <slug>` = single-workstream detail (objective, current iteration + phase, Done/Now/Next), the prior status view now behind `-w`. (Evidence: detail output for lifecycle.)
- [x] 7. Non-negotiables + ergonomics intact: no inference, no commit; single-active flows (`lume open "x"`, the transition verbs) still work with no `-w`; commands touch only intended files; gates/transitions/snapshot-derivation unchanged. (Evidence: e2e open->accept on lifecycle with no -w; git scope; grep for net/model imports.)
- [x] 8. Tests reworked, not dropped: new tests cover `-w` targeting, sole-active default, zero/multi-active errors, closed/nonexistent-target errors, the three queue buckets, the `-w` detail view, and new/close not touching a cursor. 001's current-pointer tests are rewritten to the new model (not deleted for convenience). Full suite green, 0 failures. (Evidence: test count + run.)

## Self-review
Adversarial pass against real evidence:
1. PASS - `_extract_target` pulls `-w`/`--workstream <slug>` from anywhere in argv. Live: both `lume -w lifecycle status` and `lume status -w lifecycle` resolve lifecycle; `test_w_flag_targets_anywhere` covers before/after the verb.
2. PASS - `Repository.workstream(None)` defaults to the sole active: live `lume status -w` / transitions on lifecycle work with no `-w` (build-lume closed -> lifecycle is the sole active). zero-active -> NoWorkstreamError; multi-active -> GateError listing slugs + `-w` (`test_default_*`, `test_open_multi_active_needs_w` asserts both names + `-w` in the message).
3. PASS - `workstream(slug)` raises NoWorkstreamError for an unknown slug and GateError for a closed one (`test_target_unknown_slug_errors`, `test_target_closed_slug_refused`; CLI `test_close_default_then_zero_active` shows a closed target -> exit 1).
4. PASS - `.lume/current` removed from the repo and from the code: `current_slug`/`_set_current`/`CURRENT_FILE`/`close_current` are deleted (grep finds no references); `lume new` writes only objective.md (no cursor - `test_new_creates_active_no_cursor`); `lume close` flips status only.
5. PASS - `lume status` with no `-w` renders the queue: AWAITING YOU (latest iteration in handback), IN PROGRESS (other active + latest phase / "(no iterations)"), CLOSED. Live output showed lifecycle in-progress (004 working) + build-lume closed; `test_status_queue_buckets` asserts all three buckets incl. the no-iterations case.
6. PASS - `lume status -w <slug>` is the single-workstream detail (objective/iteration/Done-Now-Next), the prior view behind `-w`. Live detail for lifecycle; `test_status_w_is_detail_not_queue` asserts it is detail, not the queue.
7. PASS - no inference/network imports (grep clean); no commits; single-active flows work with no `-w` (`test_e2e_single_active_no_w`: open->approve->start->handback->accept on the sole active); gate/transition/snapshot logic in workstream.py unchanged (that file's diff is all from 001). git scope: cli.py + repository.py + the test files; `.lume/current` was untracked, so its removal is traceless.
8. PASS - `python3 -m unittest discover`: Ran 61 tests, OK. New `test_cli.py` (9 cases) covers `-w`, queue buckets, detail, new-no-cursor, e2e, close. `test_selection.py` rewritten from the pointer model to the `-w`/default model (not deleted); `test_repository.py`'s resolution tests moved to sole-active. test_auto_snapshot/test_workstream_open/test_transitions construct Workstream directly and were unaffected.

Diff swept: no dead code (pointer methods removed, not orphaned); `_render_status` was split into `_render_detail` + `_render_queue`; no unused imports (CLOSED used by `close`).

## Handback
What changed (uncommitted):
- `repository.py`: retired the `.lume/current` pointer. `workstream(slug=None)` now resolves by `-w` slug (named errors for unknown/closed) or defaults to the sole active (named errors for zero / several, the latter listing slugs). New `workstreams`/`active_workstreams`; `create_workstream` no longer writes a cursor; `close_current` removed (close is now `ws.set_status(closed)` on a resolved target).
- `cli.py`: `-w`/`--workstream` extracted anywhere in argv; `lume status` (no `-w`) renders the cross-workstream review queue, `lume status -w <slug>` the single-workstream detail; `new` wording no longer claims "current"; `close` resolves a target then flips status.
- Tests: new `test_cli.py`; `test_selection.py` rewritten to the new model; `test_repository.py` resolution tests -> sole-active. 61 total, green.
- Migration: `.lume/current` deleted from the repo. lifecycle is the sole active workstream, so the loop continues to run with no `-w`.
This reworks 001 (decision f): the status field + new/close are kept; only the single-current cursor is replaced by the queue + `-w` target.

## Verdict
(operator: accept / reject + reasons)
2026-06-09 | ACCEPTED
