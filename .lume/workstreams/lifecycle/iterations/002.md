---
id: 002
type: execution
phase: accepted
opened: 2026-06-09
---
# Iteration 002 - Discovery: shape of the explicit lifecycle

## DoD

- [x] 1. Grounded, not invented: discovery.md maps build-lume's actual iterations 001-006 onto the four candidate stages (discovery / planning / execution / close-out), citing each iteration - establishing the stages are extracted from real practice, not imagined. (Evidence: a table in discovery.md, every build-lume iteration placed.)
- [x] 2. Core design fork named with a recommendation: it frames the central representation question - is the lifecycle a workstream-level phase machine (discovery->planning->execution->close-out) that constrains which iteration types are legal, a per-iteration `type` label, or both - states the trade-offs, and recommends one for planning to ratify. (Evidence: the fork section + a stated recommendation.)
- [x] 3. Minimal data shape sketched: for the recommended option, what fields change (e.g. iteration `type`, a workstream `phase`), how each stage's iteration differs (default template / DoD shape), and the smallest first execution slice (what 004+ would actually build). (Evidence: shape section naming concrete fields + the first build slice.)
- [x] 4. Practice-contract design test run on paper: per scope.md's seam test and the retro's flagged-unproven practice contract, the proposed shape is shown to express both members of the example pair - `iterate -> refine -> iterate` (delivery) and `discover -> research -> build -> dispose` (research) - without bespoke code per template. (Evidence: both templates written out in the proposed shape; any leak noted.)
- [x] 5. Scope held; no code: discovery produces no engine code and changes no engine behaviour; it explicitly lists what is deferred to planning (003) and execution (004+), and flags any proposed ceremony element against the constitutional "buys back more time than it costs" test. (Evidence: git shows only discovery.md + state files added; an explicit "deferred" + "ceremony cost" section.)

## Self-review
Adversarial pass against the artifact (discovery.md):
1. PASS - §1 maps all six build-lume iterations + the up-front docs onto the four stages in a cited table. Grounded finding: discovery + planning lived *outside* the loop (docs/vision, scope, constraints, questions); only execution (001-005) and close-out (006) ran inside it. So the stages are extracted from real practice, and the genuinely-missing stages are discovery + planning.
2. PASS - §2 names the fork (A workstream phase machine / B per-iteration type / C both) with trade-offs and recommends B, with rationale (smallest kill of the `type: build` hardcode; no second state machine; step-sequence-as-data per scope.md) and a stated condition for revisiting A.
3. PASS - §3 names concrete fields (widen iteration `type`; template = ordered type-list as data; per-type DoD-skeletons as data; workstream "phase" derived not stored) and the smallest first execution slice for 004: `lume open` takes a validated `type` (default execution), persists + surfaces it, nothing more.
4. PASS - §4 writes both example-pair templates as ordered type-lists over one shape (delivery = repeating execution, with reject->redo as "refine"; research = [discovery, research, build, dispose]) and records the leak: the type vocabulary must be per-template, not a fixed four-value enum - the one thing that would make the contract leak if hardcoded now.
5. PASS - §5 contains explicit deferred-to-planning and deferred-to-execution lists and a per-element ceremony-cost check. No engine code or behaviour change this iteration.

Evidence for item 5 scope: iteration 002 added only `discovery.md` (plus its own iteration file and the auto-refreshed snapshot). The engine diff visible in `git status` (cli/repository/workstream/test_repository/build-lume objective) is entirely accepted-iteration-001's selection work, still uncommitted under no-auto-commit and untouched by 002. Diff swept: discovery.md is prose only - no code, no imports.

## Handback
What changed (uncommitted):
- New `discovery.md` in the lifecycle workstream: the discovery artifact (stage mapping grounded in build-lume; the representation fork with a recommendation of per-iteration `type`; the minimal data shape + smallest 004 slice; the practice-contract design test on paper with the per-template-vocabulary leak flagged; deferred work + ceremony-cost check).
- No engine code touched.
Headline for planning (003): adopt option B (per-iteration `type`), derive the workstream "phase" rather than store it, make the type vocabulary a per-template property, and let 004 be the minimal un-hardcoding of `type` in `lume open`.

## Verdict
(operator: accept / reject + reasons)
2026-06-09 | ACCEPTED
