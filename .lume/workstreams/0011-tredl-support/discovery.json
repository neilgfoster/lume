{
  "title": "tredl-support - discovery (strategic direction)",
  "sections": [
    {
      "heading": "0. Frame",
      "body": "lume's first real consumer is tredl, an autonomous-operator layer built ON TOP OF lume. This workstream is a roadmap/planning record under a human operator: it captures the direction and reasoning so an adversarial reviewer can see that lume's unbuilt capability is staged roadmap serving demonstrated demand (tredl), not scope creep or waste. This discovery iteration records direction only - it itemises no backlog (that is the planning iteration) and builds nothing. The full reasoning and the L0-L5 backlog originate in an external seed brief (lume-backlog.seed.md / README.md, captured 2026-06-11)."
    },
    {
      "heading": "1. What tredl is (naming and the ladder)",
      "body": "Names follow lume's weaving lineage (loom -> lume). lume = the loom (the machine that weaves one piece); the human operates each iteration gate. tredl = the treadle (the control the weaver works, over and over, to drive the loom); it is the autonomous-operator layer - the SAME lume loop with Claude Code as the operator for the per-iteration gates. Automating the treadling is exactly removing the repetitive operator overhead, which is tredl's whole purpose: tredl decides which workstreams advance and drives the gates (approve -> start -> handback -> accept). Jaqard = the Jacquard head (the programmable automation of the loom's pattern control); reserved for a parked TOP layer (full autonomy / confidence-based acceptance), NOT built now. The ladder loom -> tredl -> Jaqard maps onto a strict -> confidence autonomy progression."
    },
    {
      "heading": "2. Vision and hard rules",
      "body": "lume is a deterministic, operator-gated iteration loop: an operator approves and accepts each iteration before/after work; today the operator is a human. tredl is that same loop with Claude Code as the per-iteration operator, demoting the human to the SPRINT boundaries (define the goal at the start, review the delivered PR(s) at the end). A 'sprint' is a lume workstream that decomposes into and spawns child workstreams, which tredl drives autonomously. HARD RULES: (1) tredl uses lume; tredl NEVER modifies lume - that would break lume's operator-in-the-loop contract. Instead tredl emits discoveries (capability gaps found in real use). (2) lume's principle holds: an operator is always in the loop; tredl just proves the operator need not be human at the iteration level. (3) tredl dogfoods - it builds other repos and builds itself."
    },
    {
      "heading": "3. The feedback cycle (discovery-driven)",
      "body": "1) lume helps build tredl (human-supervised lume loop). 2) tredl, in use, hits a lume capability gap, records a discovery in its repo (discoveries/*.json), then works around the gap locally so it is not hard-blocked. 3) lume scans adopter repos (via ADOPTERS) for discoveries and pulls them into its backlog. 4) A human supervises lume closing the discovery and shipping a release. 5) tredl detects the resolved discovery, adopts the new capability, removes its workaround. 6) tredl advances, finds new gaps, and the cycle repeats. Discoveries are first-class in lume: lume ALSO captures discoveries on ITSELF from its own operator's feedback, so the same channel grows lume's backlog whether the source is lume's operator or an adopter. One schema, one scan, two sources. The discovery is therefore an API between repos - design its schema and scan with the rigor lume applied to its store seam."
    },
    {
      "heading": "4. UNVERIFIED PREMISES - assumptions to confirm in source (NOT confirmed here)",
      "body": "Two premises are inherited from external assessment, not yet confirmed against lume's source. They are recorded as ASSUMPTIONS to verify, each inside the relevant L-item's own discovery iteration before any build - not as established fact. (A) 'DoD is write-only': assessment indicates dod_artifact is persisted but not EVALUATED by any verb - in particular not read at the accept gate. If true, the accept gate for an autonomous operator is only an assertion, not a check. This is L2's first task to confirm or refute. (B) 'No parent field on workstreams': assessment indicates the workstream schema has no parent reference, so a sprint that spawns child workstreams is not representable. This is L3's first task to confirm or refute. NOTE: premise (B) is trivially checkable (the workstream entity schema exposes slug/title/status/objective_artifact and no parent property) and looks true on inspection, but per the operator's instruction it is carried as an assumption to confirm inside L3's discovery rather than struck here, so the strategy stays auditable and no build proceeds on an unverified gap."
    },
    {
      "heading": "5. Decisions locked (recorded here, logged via lume decide)",
      "body": "(1) DISCOVERY CHANNEL: in-repo discoveries/*.json - it matches lume's 'state as JSON in the repo' philosophy, is contract-testable, and adds no external coupling. NOT GitHub issues. (2) AUTONOMOUS ACCEPTANCE, v0.1: STRICT. tredl auto-accepts only when the Definition of Done is machine-verifiable (tests pass, build green, lint clean, schema validates); anything subjective escalates to the human. No confidence thresholds in v0.1 - that is how autonomous agents ship confident garbage. (3) TEMPLATE SPLIT: lume owns the MECHANISM to instantiate a workstream/phase structure from a declarative pattern definition; the consumer (tredl) owns the INTELLIGENCE of choosing/authoring a pattern. Built-in named templates are content on top - grow that library only by demonstrated need, never speculatively."
    },
    {
      "heading": "6. The autonomy ladder (each layer lifts the human one rung)",
      "body": "lume: human operates each ITERATION gate. tredl: human operates each SPRINT boundary; tredl operates iterations (strict-verifiable only). Jaqard (PARKED): human operates at the PORTFOLIO/GOAL level; confidence-based subjective acceptance lives here, quarantined behind the most oversight per unit of work. Jaqard is explicitly NOT built now and may turn out to be the same engine as tredl at a higher altitude with one flag flipped (strict vs confidence). Do not design three codebases for what might be one engine at three altitudes; decide only after tredl v0.1 exists."
    },
    {
      "heading": "7. Soft spots to keep watching (for the adversarial reviewer)",
      "body": "1) REPLACING THE TRUST GATE WITH THE THING IT GUARDED: tredl-as-operator only has value if its acceptance is independent and verifiable - hence the keystone is making lume's DoD machine-evaluable and actually read at accept (premise A; verify and fix in lume FIRST). 2) AUTONOMY BOUNDED BY VERIFIABILITY, NOT AMBITION: verifiable DoD -> tredl accepts; non-verifiable -> human escalation. A constraint, not a preference. 3) BOOTSTRAPPING: lume cannot yet support tredl and tredl cannot discover its own prerequisites before it runs; the human hand-seeds the minimum capability for iteration one - the discovery loop is the steady state, not the bootstrap. 4) DEADLOCK RISK: if tredl hard-blocks on a lume gap it stalls; tredl must always have a local workaround so a discovery is the 'do it properly later' track. 5) DISCOVERY IS AN API BETWEEN REPOS: design its schema and scan rigorously; this boundary is where clean separation rots if sloppy. 6) SCOPE CREEP: an orchestration layer is the highest-risk category for sprawl - hold the v0.1 line hard and keep the template library demonstrated-need-only."
    },
    {
      "heading": "8. Scope held / deferred",
      "body": "This iteration produces discovery.json only. No engine code; no lume behaviour change. DEFERRED to the PLANNING iteration: itemise the backlog L0 -> L1/L2/L3 (capability prerequisites, L2 the keystone) -> L4 (delivery), with L5 (declarative pattern instantiation) recorded as DEFERRED / discovery-triggered and NOT scheduled. DEFERRED to each L-item's OWN future workstream: building anything, and verifying that item's assumptions in source via its own lead discovery iteration. lume's focus and operator-in-the-loop principle are unchanged by recording this roadmap."
    },
    {
      "heading": "L0.1 Primitive inventory - on-disk shapes and read/write verbs (iteration 003)",
      "body": "Ground-truth inventory of the seven primitives, each cited to source under plugin/src/lume. SEED: not a standalone artifact - a boolean flag workstream.seed in state.json; written once by 'lume seed' (cli/handlers/lifecycle.py via repository.create_workstream, sets seed=True), read only by validation. Write-mostly. WORKSTREAM: state.json -> workstream section {id, seed?, slug, title, status[active|closed], objective_artifact} (schemas/workstream.json). Written by new/close/reopen (workstream.set_status); read by status/get/snapshot. ITERATION: split across state.json -> iterations[] {id, type, phase, opened, title, verdicts[], dod_artifact} (schemas/iteration.json) AND a content file iterations/NNNN-slug.json {id, dod{preamble, items[{text, checked}]}, self_review, handback} (schemas/iteration_content.json). State entity written by open + every transition (approve/start/handback/accept/reject/redo); read by status/snapshot/get and by transition (reads current phase). Content file written at open and by handback/self_review authoring; see L0.2 for its read status. DISCOVERY: discovery.json {title?, sections[{heading, body}]} (schemas/discovery.json); only ever WRITTEN (authored directly, or one-shot by 'lume migrate' from discovery.md). No verb reads it back - confirms GAP L1 (see L0.3). PLAN: state.json -> plan[] {id, type, iter|null, tag[committed|optional], sketch} (schemas/plan_item.json); written by 'plan add'/'plan link'; read by status/snapshot/get. DECIDE: decisions.json {entries[{date, context, decision, rationale}]} (schemas/decisions.json); 'lume decide' reads-then-appends-then-writes (the one true read-modify-write verb). SNAPSHOT: not persisted at all - a pure computed markdown view (snapshot.build_snapshot) derived from iterations[] + plan[]; emitted by 'lume snapshot' and 'lume status', never written to disk."
    },
    {
      "heading": "L0.2 PREMISE A (DoD write-only / not evaluated at accept) - CONFIRMED TRUE",
      "body": "TRUE. The DoD lives in the iteration content file (iterations/NNNN-slug.json), written at open via parse_dod_items and validated on write (workstream.py validate_entity('iteration_content')). The phase machine is a fixed phase-tuple table in iteration.py: 'accept': ('handback','accepted'), 'handback': ('working','handback'), etc. transition() (workstream.py) checks ONLY that the current phase matches the source of the verb's tuple and appends a verdict; it never loads the content file, never inspects dod.items[].checked, and never reads self_review/handback to gate. A grep across plugin/src/lume for reads of 'dod'/'checked'/'iteration_content' finds only writes (open, migrate) and schema validation - zero decision-reads. CONSEQUENCE: an iteration can pass through accept regardless of whether its DoD items are checked; the accept gate is an operator assertion, not a machine check. This is exactly the keystone L2/P3 must fix, and it is now confirmed in source rather than assumed."
    },
    {
      "heading": "L0.3 PREMISE B + GAPs L1/L4 - all CONFIRMED TRUE; iteration/phase enums recorded",
      "body": "PREMISE B (no parent ref on workstream): TRUE. schemas/workstream.json properties are exactly {id, seed, slug, title, status, objective_artifact} - no parent_id/children/parent property; iterations[] and plan[] are sibling arrays in state.json, not a hierarchy of workstreams. A sprint-that-spawns-workstreams is not representable today (P4/L3). GAP L1 (no cross-repo discovery mechanism): TRUE. discovery.py only defines parse_discovery_md for one-shot migration; no verb scans ADOPTERS or any external repo, and 'ADOPTERS' does not appear anywhere in plugin/src/lume. Discovery is purely an in-repo iteration type + hand-authored discovery.json; the discoveries/*.json capture+scan API (P2/L1) does not exist. GAP L4 (no delivery): TRUE. No .github/ (no CI workflow), no pyproject.toml, no setup.py/setup.cfg at repo root; only .claude-plugin/ descriptor, plugin/ source tree, tests/, and docs. No version/packaging/release config (P5/L4). ENUMS verbatim (schemas/iteration.json): type = [discovery, planning, execution, closeout]; phase = [proposed, approved, working, handback, accepted, rejected]. Scope held: no engine/product code changed in this iteration - reads only."
    },
    {
      "heading": "P3.1 Premise A at design depth - the exact accept hook point (iteration 004)",
      "body": "Confirmed where a DoD evaluation must hook in. The phase machine is the table iteration.TRANSITIONS = {approve:(proposed,approved), start:(approved,working), handback:(working,handback), accept:(handback,accepted), reject:(handback,rejected), redo:(rejected,working)}. Workstream.transition(verb) (workstream.py ~236) looks up (source,target), checks iteration.phase==source (else GateError), then mutates entity['phase']=target and appends a verdict; it never loads the iteration_content doc. The CLI handler handle_transition (cli/handlers/lifecycle.py ~102) calls ws.transition(ctx.cmd) and a GateError already maps to exit 1. THEREFORE the minimal, idiomatic hook is: in transition(), when verb=='accept', BEFORE the phase mutation, load the current iteration's content doc, evaluate its DoD checks, and raise GateError(listing failures) if any fail. This reuses the existing refuse-with-exit-1 path; no handler/CLI change is needed for the refusal to surface. Confirms Premise A as a design fact: today nothing reads dod.items at accept, and this is the one function to change."
    },
    {
      "heading": "P3.2 Design: verifiable-check schema (additive to iteration_content)",
      "body": "Add an OPTIONAL 'check' object to each dod item in schemas/iteration_content.json. An item stays {text, checked} and may additionally carry {check}. Three predicate kinds for v0.1 (the strict, demonstrably-deterministic set): (1) command - {kind:'command', cmd:'<shell>'}; passes iff the command exits 0. (2) file-exists - {kind:'file-exists', path:'<repo-relative>'}; passes iff the path exists. (3) schema-valid - {kind:'schema-valid', entity:'<lume schema name>', path:'<repo-relative json>'}; passes iff the file loads and validate_entity(entity, doc) succeeds (reuses the existing validate seam). BACKWARD-COMPATIBLE: 'check' is optional, so every existing iteration_content doc (items with only text+checked) still validates and is treated as prose-only. No migration. Explicitly OUT of v0.1 (scope-creep traps): arbitrary predicate languages, regex-over-output, HTTP probes, confidence scores."
    },
    {
      "heading": "P3.3 Design: accept-time semantics, verifiability status, and the autonomy split",
      "body": "Two distinct responsibilities, kept separate. (A) MACHINE VETO (lume enforces): at accept, every item carrying a 'check' is evaluated; if ANY verifiable check fails, accept is REFUSED via GateError (phase stays handback) and the error names the failing items. If all verifiable checks pass (or there are none), accept proceeds - it remains the OPERATOR'S decision, exactly as today. lume never AUTO-accepts; it only gains the power to REFUSE on a failed machine check. (B) VERIFIABILITY STATUS (lume exposes, consumer decides): each item is classified verifiable (has a check) or non-verifiable (prose-only). A DoD is fully-verifiable iff every item has a check. lume surfaces this status so an autonomous operator (tredl) can apply decision #2 from iteration 001 (STRICT autonomous acceptance): auto-accept ONLY when the DoD is fully-verifiable AND all checks pass; ANY prose-only item -> escalate to a human. The INTELLIGENCE of that choice lives in tredl; lume only provides the veto + the status. This is the keystone that makes tredl-as-operator independent and verifiable rather than a rubber stamp."
    },
    {
      "heading": "P3.4 Open forks for the planning iteration (decided vs deferred)",
      "body": "DECIDED here (logged via lume decide): the hook point (transition/accept + GateError), the additive 'check' field, the three v0.1 predicate kinds, the veto-vs-status split, and full backward-compatibility for prose-only DoDs. DEFERRED to planning/execution: (1) SURFACE - how verifiability status + a dry-run are exposed: leaning to a new pure evaluator that accept calls AND a 'lume check' verb for dry-run (so you can see pass/fail without transitioning), plus a status flag; confirm in planning. (2) COMMAND EXECUTION model - cwd (repo root), timeout (a default, e.g. 120s), env, and whether command checks run at all in untrusted contexts; a sandbox/allowlist is a candidate DISCOVERY-triggered follow-up, NOT v0.1 - record the trust assumption (the DoD author == the operator, same trust as running the test suite). (3) AUDIT - whether evaluated check results are persisted (e.g. alongside the verdict) for later inspection, or recomputed. (4) ERROR SHAPE - the exact GateError message/JSON for failed checks. Scope held: iteration 004 is discovery - no engine code was written; the build is the execution iteration."
    },
    {
      "heading": "P5.1 Delivery inventory (iteration 010) - cited current state",
      "body": "Ground truth on how lume ships today. CI: NONE - no .github/ directory, no workflow; PRs (incl. #19/#20/#21) merge with only local pytest as evidence. VERSION: a SINGLE source - plugin/.claude-plugin/plugin.json line 3, '\"version\": \"0.1.0\"'; the string appears nowhere else in shipped config. DISTRIBUTION CHANNEL: a Claude Code plugin, NOT a PyPI package. .claude-plugin/marketplace.json declares one plugin with source './plugin'; install is '/plugin marketplace add neilgfoster/lume' then '/plugin install lume@lume' (README Install). lume is stdlib-only - no runtime deps, no build step. RELEASE/TAGS: NONE - 'git tag' is empty; no GitHub release. INSTALL VERIFICATION: tests/test_smoke_install.py already drives the bundled bin/lume in a fresh temp dir end to end (seed -> gate the seed iteration -> open a workstream), exercising the real entry point + src-layout path resolution; the README flags the live-Claude-Code install as [U] (verified by smoke test, not yet by a human in a live session)."
    },
    {
      "heading": "P5.2 P5 DONE-WHEN confirmed/REFINED against reality",
      "body": "(1) CI runs full pytest on push/PR, required: CONFIRMED as a real gap - build it. (2) 'a pyproject declares the package and version': REFINED. lume does not ship to PyPI; its package identity and version live in plugin.json and its distribution is the marketplace plugin. A pyproject.toml would declare a Python package nobody pip-installs, duplicating the version and risking drift/misleading adopters. REFINEMENT: keep plugin.json as the single version source; do NOT add a pyproject solely to satisfy the clause. (If a pyproject is ever wanted for tooling/editable installs, it must READ or be checked against plugin.json, never become a second source of truth.) (3) 'a tagged, semver release matching the declared version': CONFIRMED and is the real 'packaging' for this channel - a git tag vX.Y.Z whose value equals plugin.json's version is what an adopter pins. (4) plugin install path verified end to end from clean state: PARTIALLY met by test_smoke_install (entry point) - the residual gap is the live-Claude-Code '/plugin install' path, currently [U]. (5) 'a release artifact an adopter can pin to': REFINED - for a git-repo marketplace the pinnable artifact IS a git tag/ref (and optionally a GitHub release), not a built archive; 'marketplace add' against a tag/SHA is the pin."
    },
    {
      "heading": "P5.3 Open forks for the planning iteration",
      "body": "DECIDED (findings): version stays single-sourced in plugin.json; the pinnable artifact is a git tag == that version; no pyproject added merely to tick the clause. FORKS for planning: (A) CI SHAPE - GitHub Actions workflow running 'python -m pytest -q' on push + PR across supported python versions (which? lume targets stdlib; pick a minimum, e.g. 3.10+, and confirm against syntax used); make it a required status check (branch protection is the operator's to set). (B) VERSION/TAG CONSISTENCY - a check (ideally a DoD command-check, dogfooding P3) that the git tag equals plugin.json version on release; where it runs (CI release job vs a 'lume'-side script). (C) RELEASE PROCESS - tag -> GitHub release; whether to attach a built archive of plugin/ or rely on the tag/source; keep it minimal. (D) INSTALL VERIFICATION - what to add beyond test_smoke_install to close the [U] live-install caveat (documented manual run from clean state, or a CI job that does 'claude --plugin-dir'? the latter needs the Claude CLI in CI - likely out of scope, so probably a documented, reproduced manual verification). (E) SCOPE GUARD - hold to delivery; do not let this become a packaging-for-PyPI project. Scope held: iteration 010 is discovery - no .github, no pyproject, no engine code written; building is deferred to planning/execution."
    },
    {
      "heading": "P2.1 Naming collision - resolution",
      "body": "lume already overloads 'discovery' twice: a 'discovery' iteration TYPE, and a per-workstream .lume/.../discovery.json (the findings artifact, schema = {title, sections}). The P2 cross-repo capability-gap record is a THIRD distinct concept. RESOLUTION (recommended, for planning to ratify): name the machine entity 'gap' (a capability gap), with its own schema; keep the human word 'discovery' in prose only. Records live at a repo ROOT in 'gaps/<id>.json' (one file per gap, matching the glob the strategy assumed and easing an adopter's add-one-record PR). This avoids a third 'discovery' meaning in schemas/verbs/docs. CONSEQUENCE: ADOPTERS.md's note that lume reads 'discoveries/*.json' should be updated to 'gaps/*.json' (or the dir kept as 'discoveries/' with entity 'gap' if the operator prefers the strategy's wording - the ONE naming point planning must settle)."
    },
    {
      "heading": "P2.2 Gap record schema + capture",
      "body": "SCHEMA (gaps/<id>.json), fields with rationale: id (string, stable within the source repo - dedupe component); source (string, the source repo identifier/URL - dedupe component); title (string); context (string - what the gap is and why it blocks); status (enum open|acknowledged|resolved); created (ISO date); resolution (string|null - link/ref when resolved). DEDUPE KEY = (source, id), so the same gap re-read on a later scan is recognised, not re-ingested. CAPTURE: one verb records a gap in the CURRENT repo's gaps/ dir (the same artifact lume writes about itself AND an adopter writes about lume - one schema, two sources). Per-file (not a single gaps.json) so adopters can PR a single record and so each carries its own status. lume records gaps on itself the same way it ingests adopters'."
    },
    {
      "heading": "P2.3 Scan + ingest via git worktrees (operator decision)",
      "body": "REACH = git clone/fetch + git worktree (operator decision; NOT local sibling paths). MECHANISM: (1) parse ADOPTERS.md's fixed table for repo URLs (Link column); (2) per adopter, clone-if-absent / fetch-if-cached into a cache, then add a git WORKTREE at the target ref (default branch) to get a clean checkout; (3) read gaps/*.json from the worktree; (4) ingest records with status=open; (5) IDEMPOTENT - dedupe on (source, id) against what lume already holds, so reruns add nothing new; (6) remove/prune the worktree after reading (ephemeral), keeping the fetched repo cached for speed. FAILURE: a missing/unreachable adopter repo degrades gracefully (skip + report), it does not abort the whole scan. This reach is itself a candidate for lume's EnterWorktree-style isolation but at the engine level uses plain 'git worktree add/remove'."
    },
    {
      "heading": "P2.4 Landing target + open forks for planning",
      "body": "LANDING (recommended, fork to ratify): ingested gaps land in a lume-SIDE backlog store (lume's own gaps/ records marked 'acknowledged', referencing source+id) - NOT auto-created plan items. Turning an acknowledged gap into a workstream/plan item stays an operator triage step, preserving operator-in-the-loop (lume ingests and surfaces; the human decides what to build). FORKS for planning: (a) confirm the naming (gaps vs discoveries dir); (b) the lume-side landing store shape + where it lives; (c) worktree cache location (e.g. a .lume/cache or out-of-tree) + lifecycle/cleanup details; (d) STATUS TRANSITIONS - who moves open->acknowledged (lume on ingest) and ->resolved, and how a resolution SIGNALS BACK to the adopter (lume publishes its resolved gap; the adopter re-scans / learns - full round-trip likely deferred past v0.1); (e) contract-test shape for ingest + dedupe + status. Scope held: iteration 017 is discovery - no engine code; findings + decisions only."
    },
    {
      "heading": "P4.1 Parent reference - design",
      "body": "Premise B re-confirmed (L0): schemas/workstream.json is {id, seed, slug, title, status, objective_artifact} - no parent. DESIGN: add an OPTIONAL 'parent' field to the workstream section = the PARENT workstream's id (string). A child stores its parent; CHILDREN are DERIVED by scanning all workstreams' state for parent==this id (single source of truth - no duplicated children array to drift, mirroring how iterations link to plan items by iter id and gaps key by source). Backward-compatible: existing workstreams have no parent and are roots. Validation: parent must reference an existing workstream; a workstream may not be its own parent, and the parent chain must be acyclic (no ancestor cycle)."
    },
    {
      "heading": "P4.2 Spawn - design",
      "body": "DECIDED (recommended): a new verb 'lume spawn <slug> \"<title>\"' creates a child workstream whose parent is the -w target (or the sole active workstream). It reuses create_workstream then sets parent on the child's workstream section. Rationale: a dedicated verb reads as the 'sprint spawns sub-work' intent and keeps 'lume new' (root creation) simple; the alternative 'lume new --parent <id>' overloads new. Guards at spawn: parent exists (it's the target, so yes), and the new slug is unique (create_workstream already enforces). Cycle prevention is trivial for spawn (child is brand-new) but the validator must still reject a parent edit that would form a cycle."
    },
    {
      "heading": "P4.3 Rollup - design",
      "body": "DECIDED (v0.1 scope): (a) a parent's single-workstream detail ('lume status -w <parent>') lists its CHILD workstreams with each child's current iteration phase + status; (b) the cross-workstream queue ('lume status') NESTS children under their parent (indented) so the tree is visible. DEFERRED past v0.1: aggregate metrics/percent-complete rollup and recursive multi-level summarisation beyond direct children - keep v0.1 to one level of visible children + their phase, not a computed burn-up. Rollup is READ-ONLY derivation (scan children), no new stored state."
    },
    {
      "heading": "P4.4 Close-guard + forks",
      "body": "DECIDED (recommended): 'lume close' on a parent with one or more ACTIVE (non-closed) children is REFUSED with a clear GateError naming the open children - NOT a cascade (cascading close would silently end child work and break the operator-in-the-loop principle). Closing is allowed once all children are closed; reopen is unaffected (reopening a child under a closed parent is allowed but flagged as a fork to confirm). FORKS for planning: (1) verb shape (spawn vs new --parent) - recommended spawn; (2) rollup depth - recommended one level + phase, queue nesting; (3) close-guard - recommended refuse; (4) whether reopen of a child requires the parent active; (5) how 'lume status' renders deep trees (indent vs flat-with-parent-column). Scope held: iteration 023 is discovery - no engine code."
    }
  ]
}
