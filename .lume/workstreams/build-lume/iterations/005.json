{
  "dod": {
    "items": [
      {
        "checked": true,
        "text": "1. A successful `open` and every successful transition verb (approve/start/handback/accept/reject/redo) regenerate snapshot.md as part of the same command - no separate `lume snapshot` call needed for Done/Now/Updated to reflect the new state. (Evidence: run a transition; snapshot.md's Now + Updated show the new phase without a separate call.)"
      },
      {
        "checked": true,
        "text": "2. The standalone `lume snapshot` command still exists (for refreshing after editing `## Next` or iteration titles). (Evidence: run it.)"
      },
      {
        "checked": true,
        "text": "3. The folded refresh preserves `## Next` and stays idempotent: re-running the same no-op transition path does not churn the file beyond the intended phase change. (Evidence: Next intact after a folded refresh.)"
      },
      {
        "checked": true,
        "text": "4. No behaviour regression: gate rules, verdict notes, and byte-for-byte iteration-body preservation are unchanged. (Evidence: the existing suite still green.)"
      },
      {
        "checked": true,
        "text": "5. A folded refresh writes only snapshot.md in addition to the command's existing iteration-file write; no inference in the path. (Evidence: git status shows just the iteration file + snapshot.md; source.)"
      },
      {
        "checked": true,
        "text": "6. New tests cover: `open` refreshes the snapshot, a transition refreshes it (Now phase updated), and the standalone verb still works. All green. (Evidence: test count, 0 failures.)"
      },
      {
        "checked": true,
        "text": "7. End to end: open -> approve -> start -> handback -> accept leaves snapshot.md current at every step with zero `lume snapshot` calls. (Evidence: scripted run showing Now after each step.)"
      }
    ],
    "preamble": ""
  },
  "handback": "What changed (uncommitted):\n- `workstream.py`: `open_iteration` and `transition` now call `record_snapshot()` after writing the iteration file; `record_snapshot` seeds a `# <name> - snapshot` title when no snapshot file exists yet. Docstrings updated.\n- Tests: new `test_auto_snapshot.py` (5 cases). 41 total, green.\n- No CLI surface change: the verbs are the same; they just keep the snapshot current automatically. Standalone `lume snapshot` retained.\nThis is the last residual manual step removed - I no longer run `lume snapshot` after a transition. The only hand-authored part of the snapshot is now `## Next`.",
  "id": 5,
  "self_review": "Adversarial pass against real evidence:\n1. PASS - sandbox e2e drove open -> approve -> start -> handback -> accept with no `lume snapshot` calls; `## Now` tracked each phase (proposed/approved/working/handback/accepted) and `Updated:` followed. The fold lives in `Workstream.open_iteration` and `transition`, which call `record_snapshot()` after writing the iteration file.\n2. PASS - the standalone `lume snapshot` verb is unchanged and still regenerates on demand (`test_standalone_snapshot_still_works`); needed after editing `## Next` or titles.\n3. PASS - `test_next_section_survives_folded_refresh`: hand-edited `## Next`, drove a transition, Next intact. `build_snapshot` remains idempotent (unchanged from 004).\n4. PASS - no regression: the full suite (now 41) is green, including 003's gate/verdict/byte-for-byte tests and 004's snapshot tests. Folding added writes to snapshot.md only; iteration-body preservation tests still pass because the iteration file write is unchanged.\n5. PASS - a folded command touches exactly the iteration file + snapshot.md (git status after a transition shows just those two). No inference; date from the injected clock.\n6. PASS - new `test_auto_snapshot.py`: open refreshes, transition refreshes Now's phase, full loop stays current without explicit snapshot, Next survives, standalone still works.\n7. PASS - see item 1 evidence (the scripted Now-after-each-step run).\n\nDocstrings for `open_iteration`/`transition` updated to state they refresh the snapshot (the 003-era \"touches only the current iteration file\" wording is now superseded - noted here so the change is explicit). Diff swept: no unused imports; the fold reuses the existing `record_snapshot`/`build_snapshot` path rather than duplicating logic."
}
